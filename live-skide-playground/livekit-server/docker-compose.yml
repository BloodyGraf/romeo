version: "3.9"

services:
  livekit:
    image: livekit/livekit-server
    container_name: livekit
    command: >
      --dev
      --bind 0.0.0.0
      --node-ip 0.0.0.0
    ports:
      - "7880:7880"   # WebSocket / API
      - "7881:7881"   # WebRTC